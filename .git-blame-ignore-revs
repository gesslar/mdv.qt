# Commits listed here are skipped by `git blame` so bulk, no-logic changes
# (mass reformats, renames) don't mask who actually authored each line.
#
# List full 40-char commit SHAs, one per line. Add a commit AFTER you've made
# it (the SHA can't exist in the same commit it names) — so: run `make format`,
# commit it, then paste its hash here in a follow-up commit.
#
# Active locally once per clone via:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# GitHub honors this file automatically.

# clang-format the tree to .clang-format (+ introduced the lint tooling)
91d458dc47bf8d5fad35ffd2353deff3d0980eab
